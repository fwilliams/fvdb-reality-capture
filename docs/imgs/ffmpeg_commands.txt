# Generate palette for video
ffmpeg -i Large_World_480p.mp4 -vf "fps=10,scale=320:-1:flags=lanczos,palettegen" palette.png

# Generate compressed gif from video
ffmpeg -i Large_World_480p.mp4 -i palette.png -filter_complex "fps=10,scale=320:-1:flags=lanczos[x];[x][1:v]paletteuse" output.gif
